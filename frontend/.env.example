# Puerto del servidor de desarrollo de Vite
PORT_FRONTEND=5173

# Backend al que Vite reenvía las llamadas a /api durante el desarrollo
BACKEND_URL=http://localhost:4000

# Solo para despliegues donde el frontend y el backend viven en dominios
# distintos. En desarrollo se deja vacío y se usa el proxy de arriba.
# VITE_API_URL=https://api.mi-tienda.com/api
